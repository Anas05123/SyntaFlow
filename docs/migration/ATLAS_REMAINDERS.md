# CoreDesk — Atlas Remainder Audit Log

> **Status:** AUDITED & APPROVED  
> **Date:** 2026-09-13  
> **Auditor:** Migration Agent  

This document logs every remaining case-insensitive occurrence of the string `atlas` across the workspace, along with the technical or historical justification for its presence.

**Rule:** Zero unexplained Atlas references may exist in active product code.

---

## 1. Audited Remainder Inventory

| Category | File / Path | Remainder Syntax | Justification & Lifecycle |
|---|---|---|---|
| **Migration Syntax** | `database/migrations/0001_foundation_metadata.sql` (Lines 7, 15) | `-- atlas:statement-breakpoint` | **Checksum Stability:** Delimiter generated by Drizzle/Atlas CLI migration tooling. Modifying this string would alter the SHA-256 checksum of existing applied migrations (`0001_foundation_metadata`). `database/scripts/migrate.mjs` supports both `coredesk` and `atlas` breakpoints. |
| **CLI Fallback** | `database/scripts/status.mjs`<br>`database/scripts/migrate.mjs`<br>`database/scripts/backup.mjs` | `process.env.PROJECT_ATLAS_DATA_PATH`<br>`atlas.sqlite` | **Backward Compatibility:** CLI utilities prioritize `COREDESK_DATA_PATH` and `coredesk.sqlite`, but cleanly fallback to `PROJECT_ATLAS_DATA_PATH` and existing `atlas.sqlite` files on developer machines to prevent data disconnects. |
| **Test Environment** | `tests/integration/database-scripts.spec.ts` | `PROJECT_ATLAS_DATA_PATH: dataRoot` | **Fallback Verification:** Passes both `COREDESK_DATA_PATH` and `PROJECT_ATLAS_DATA_PATH` to ensure dual-path environment variable resolution functions in CI. |
| **Seed Fixture** | `apps/desktop/src/domain/seed.ts` (Lines 85-89) | `id: 'cl-atlas'`<br>`name: 'Atlas Coffee Roasters'` | **Fictional Mock Data:** Sample client entity ("Atlas Coffee Roasters") used for UI preview and demo data. Does not represent product or system naming. |
| **Brand Rule** | `assets/brand/README.md` (Line 18) | `...or use the retired name "Atlas".` | **Explicit Prohibition:** Documentation rule explicitly forbidding the use of the retired product name. |
| **Historical Archive** | `docs/archive/atlas-history/**` | Various Project Atlas documents | **Historical Lineage:** Retired Project Atlas engineering handbook, strategy documents (`Project_Atlas_Product_Strategy.pdf`), and early task specs. All active architectural knowledge has been extracted to `docs/`. Notes are prefixed with the mandatory historical archive disclaimer. |
| **Migration Audit** | `docs/migration/**` | Audit and migration reports | **Audit Trail:** Historical documentation of the migration and consolidation process. |
| **Git Commit Log** | `.git/` | Past commit messages and diffs | **Immutable Git Lineage:** The Git commit history preserves all commits made under the former repository root. |

---

## 2. Active Product Verification

Outside of the audited exceptions above:
- Product identity across all active UI, menus, windows, headers, and metadata is **CoreDesk**.
- NPM packages are strictly scoped to `@coredesk/ai-engine` and `@coredesk/contracts`.
- Prompt registry identifiers use `coredesk.foundation.*`.
- Application preload bridge exposes `window.coreDeskDesktop` and `CoreDeskPreloadApi`.
- Application launcher executable is `CoreDesk.exe`.
